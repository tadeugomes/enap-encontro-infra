# Dependencias para reproduzir as analises de ML (07_scripts/ml_*.py).
# Todos os modelos usam random_state=42; rode os scripts a partir da raiz do repo.
#
# ATENCAO A VERSAO DO SCIKIT-LEARN. A Fase 4 (regressao quantilica) e sensivel
# a ela: o GradientBoostingRegressor com loss='quantile' e alpha=0.9 mudou de
# comportamento na versao 1.9.0. Varredura de jul/2026 (contra a auditoria
# publicada, com predicoes bit-identicas entre si):
#   1.5.0, 1.5.1, 1.5.2, 1.6.0, 1.6.1, 1.7.2, 1.8.0  -> 580 alertas "ALTO" (OK)
#   1.9.0                                            -> 593 alertas "ALTO" (quebra)
# Ou seja, qualquer versao >=1.5,<1.9 reproduz os numeros publicados; a quebra
# e exclusiva da 1.9.0. Mantemos 1.5.2 por ser a versao ja validada no pipeline.
# Os quantis P10 e P50 e as Fases 2 e 3 sao estaveis em todas as versoes.
pandas==2.3.2
numpy==2.3.1
scikit-learn==1.5.2
openpyxl==3.1.5
matplotlib==3.10.6
seaborn==0.13.2
joblib==1.5.3
# Necessario apenas para gerar o artigo (07_scripts/gerar_artigo_docx.py)
python-docx==1.2.0
