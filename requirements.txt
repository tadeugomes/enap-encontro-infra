# Dependencias para reproduzir as analises de ML (07_scripts/ml_*.py)
# Versoes verificadas em auditoria de reprodutibilidade (jul/2026), com o
# pipeline das fases 2 a 4 executado de ponta a ponta em macOS.
# Todos os modelos usam random_state=42; rode os scripts a partir da raiz do repo.
pandas==2.3.2
numpy==2.3.1
scikit-learn==1.9.0
openpyxl==3.1.5
matplotlib==3.10.6
seaborn==0.13.2
joblib==1.5.3
# Necessario apenas para gerar o artigo (07_scripts/gerar_artigo_docx.py)
python-docx==1.2.0
